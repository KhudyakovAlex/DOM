# DOM — учебный проект: геораспределённая система управления инженерным оборудованием

Идём **по шагам, от простого к сложному**. Одна из задач — разобраться с технологиями (стеком).

## Пример (только смотреть)
- Пример проекта: `D:\DOM\TESTVERSION`
- **ПРИМЕР НЕ ТРОГАЕМ** — только читаем и берем из него решения/стек.

## Стек (берём из примера)
- Backend: FastAPI + SQLAlchemy + Alembic + Postgres
- Frontend: Vue 3 + Vite + Pinia
- Карта: MapLibre GL + PMTiles

## Ближайшая цель (самая первая)
- Делаем **свой** самодостаточный backend в этом проекте.
- Переносим `yaroslavl.pmtiles` внутрь backend (где-то “рядом с кодом”, чтобы проект был самодостаточным).
- На **единственной** странице фронтенда показываем карту из этих тайлов.
- **Авторизацию пока не делаем**.

## Данные
- Исходный файл карты сейчас: `D:\DOM\MAP\yaroslavl.pmtiles`